# Core dependencies
python-dotenv
dlisio

# Progress tracking
tqdm==4.66.1

# Data processing
numpy
pandas

# Logging and utilities
structlog

# LAS file parsing
lasio

# Astra DB Data API client
astrapy

# Unstructured SDK for document processing
unstructured[all-docs]>=0.10.0
unstructured[pdf]>=0.10.0
unstructured[local-inference]>=0.10.0