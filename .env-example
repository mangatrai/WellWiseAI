# Database Configuration
ASTRA_DB_API_ENDPOINT=
ASTRA_DB_TOKEN=
ASTRA_DB_KEYSPACE=default_keyspace
ASTRA_DB_COLLECTION=oil_gas_documents
ASTRA_TABLE_NAME=petro_data

# Environment (optional, defaults to "production")
ENVIRONMENT=dev

# OpenAI Configuration  
OPENAI_API_KEY=
EMBED_MODEL=text-embedding-ada-002
CHAT_COMPLETION_MODEL=gpt-4o
ASTRA_OPENAI_KEY_NAME=
LOCAL_CHAT_COMPLETION_MODEL=llama3.1:8b

# Data Directories
DATA_DIRECTORY=
PARSED_DIRECTORY=structured_data
UNST_PARSED_DATA_DIR=unstructured_data

# Parser Configuration
STRUCTURED_FILE_TYPES=.las,.dlis,.csv
UNSTRUCTURED_FILE_TYPES=.pdf,.txt,.docx,.doc,.pptx,.html,.md,.png,.jpg,.jpeg
DUAL_PROCESSING_FILE_TYPES=.dat,.xlsx
SEGY_SAMPLE_INTERVAL=2500

# Processing Configuration
MAX_WORKERS=4
RETRY_ATTEMPTS=3
TIMEOUT_SECONDS=300
LOG_LEVEL=DEBUG
LOG_FILE_NAME=wellwise_parser.log

# SEGY Parser Configuration
SEGY_SAMPLE_SIZE=10000